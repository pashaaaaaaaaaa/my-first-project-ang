import { TextModificatorDirective } from './text-modificator.directive';

describe('TextModificatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextModificatorDirective();
    expect(directive).toBeTruthy();
  });
});
